# 音效交付与接入规则

## 工作范围

按用户 2026-09-18 的要求：只生成音效，交付试听文件、参数、来源和接入建议。除非用户后续明确要求，不修改游戏代码、不复制素材到游戏资源目录、不执行项目接入或发布。生成后的技术检查不等同于游戏内试听验收。

## 文件路径交付规则

每完成一个音效，必须在交付回复中提供该音效文件的完整绝对路径，并附可点击的文件链接。试听播放器不能替代文件路径。批量生成时逐个列出；如交付多个格式或版本，分别标明对应路径。

## 本次音效

- 文件：`ballistic-shot-01.wav`，短促、偏干的单次实弹枪械风格音效。
- 完整路径：`/Users/zmy/Documents/ChatGPT/CrazyGames_1/audio-delivery/ballistic-shot-01.wav`
- 设计：击发爆响、低频冲击、短空气尾音和轻微枪机机械声；取消激光式长音高滑动。
- Jfxr 0.13.0 加本地分层混音程序合成，无外部采样；这是实弹枪声的合成近似，不是真枪录音。
- PCM16 WAV，44.1 kHz，单声道，0.220 秒，19,448 字节。
- 峰值约 -5 dBFS；已检查非静音、无数值削波，首尾采样归零。
- `ballistic-shot-01.source.json` 保存全部分层参数、来源和音频 SHA-256。
- `source/render-ballistic-shot-01.mjs` 保存可复现的合成及混音流程。
- 当前为待试听确认的样本，未执行项目接入或游戏内验收。

## 已废弃设计

`laser-shot-01` 已按用户要求废弃，不再推荐接入。历史音频及参数保留作为记录；当前枪声音效候选以 `ballistic-shot-01` 为准。

## 建议接入约定（尚未执行）

| 项目 | 建议 |
| --- | --- |
| 资源标识 | `sfx_player_ballistic_shot_01` |
| 触发点 | 玩家实际开火、子弹生成或枪口闪光开始时，每次开火一次 |
| 避免重复 | 不绑定每帧更新、伤害数字刷新或每个命中目标；散射的一次齐射先只播一次 |
| 初始音量 | SFX 通道从 -10 dB 开始试听，按听感在 -14 至 -6 dB 间调整；这是播放增益，不是文件峰值 |
| 重叠控制 | 同一枪声最多同时 3 个；达到上限先丢弃新声音，避免突然截断旧声音产生爆音 |
| 高频触发 | 若高速攻击刺耳，可试 60–80 ms 最小发声间隔；只限制发声，不改变攻击逻辑 |
| 变化 | 首轮固定音色便于对比；确认后可加入约 ±3% 音高变化 |
| 开始播放 | 在玩家首次点击或按键后启用声音，遵守浏览器自动播放限制 |
| 暂停与静音 | 暂停、切到后台或静音时停止新增枪声；恢复时不补播积压声音 |
| 加载失败 | 保持游戏正常运行，不因音效加载或解码失败阻塞战斗 |
| 文件格式 | 首轮直接用这个小体积 WAV；批量素材时再按目标浏览器支持情况决定压缩格式 |

## 建议验收步骤

1. 单次射击：声音与枪口闪光对齐，没有重复播放。
2. 连续射击 30 秒：观察重叠数量并试听，检查刺耳、爆音和音量累积。
3. 静音、暂停、切后台、返回：不漏控声音、不补播历史事件。
4. 耳机和扬声器各试听一次，再决定音色及音量调整。

以上是后续接入方的测试清单，本次未执行游戏内接入或播放测试。

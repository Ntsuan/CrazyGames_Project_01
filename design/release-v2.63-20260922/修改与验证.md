# v2.63 分支与发布配置（Codex）

2026-09-22；基线v2.62 → v2.63。本次修改由Codex完成。用户同意正式版功能清单并要求在GitHub完成各分支配置。

## 分支与工程

目标仓库：Ntsuan/CrazyGames_Project_01。保留两个长期分支：codex/develop默认development，main默认basic。Full是预留构建配置，当前不能出包，不创建第三个长期分支。

实际开发源仍在 `/Users/zmy/WorkBuddy/CrazyGames`；Git管理与推送目录 `/Users/zmy/Documents/ChatGPT/CrazyGames_1`。本轮将当前游戏源代码、完整素材、版本快照、设计与交接资料同步入Git仓库；工作工具缓存和玩家测试日志不上传。旧Phaser骨架保留为历史，不作为当前运行入口。

## 行为与参数变化

| 项目 | development | basic | full |
| --- | --- | --- | --- |
| 测试日志/复位/清档/结束测试/4×8× | 保留 | 隐藏入口且函数禁止执行，停止采集和日志写盘 | 关闭 |
| 普通玩家2×/双模式/重塑/改装 | 保留 | 保留 | 计划保留 |
| 补给 | 模拟广告完成后发奖 | 在线60秒免费领取 | SDK待接 |
| 重塑双倍 | 模拟完成获得资格 | 不显示，旧模拟资格不再用于翻倍 | SDK待接 |
| 真实广告 | 无 | 无 | 尚未实现，禁止出包 |

Basic补给金额沿用round(mobGold(floor) × 20 × goldMult)，冷却60000ms；从模拟观看改为免费领取，不调奖励数额。常规重塑碎片保持1倍，15%装备回收保持。开发版行为和原数值不变。免费领取可能提高实际领取率，仍需真人长期节奏验证。

## 文件

- game/index.html：明确BUILD_PROFILE常量、统一功能开关、入口/日志/导出函数防护、Basic补给和普通重塑文案。
- game/assets/i18n/en.json、en.js：新增发布文案；CHANGELOG与index_v2.63快照。
- Git仓库release/profiles.json：分支/配置矩阵。
- scripts/neon-release.cjs：设置源配置、分支检查、运行素材依赖打包；Full未就绪禁止构建。
- scripts/neon-tests：15项配置专项、原核心76项（C75明确作为已知基线记录）、技能池38项、包检查。
- .github/workflows/neon-release.yml：两个分支与PR运行校验，上传包与验证报告，不自动发布到CrazyGames。

## 已完成验证

- 发布配置专项15/15：开发工具保留、Basic隐藏入口不可调用、普通2×/双模式/重塑保留、免费领取单次发奖/写失败/弹窗与冷却拦截、旧模拟资格处理、损坏存档不覆盖。
- 核心75/76，失败项C75跨版本日志数量4而非3与原版一致；CI只允许该已知错误签名，其余失败阻止构建，未将C75算作通过。Basic不运行该日志系统。
- 技能池38/38。包与分支检查通过：132个运行文件、约20.33MiB；只含index.html及实际依赖assets，排除日志/文档/历史页面/未引用素材。Full构建及错误分支配置均能被拒绝。
- GitHub运行结果与浏览器证据在完成推送后追加。本地/平台存档仍未隔离，不能声称已接云存档。浏览器QA使用独立端口新测试档。

## 回退与交接

本目录baseline/game保存v2.62 HTML、CHANGELOG、英文词典；回退恢复这些文件及相应词典缓存引用，保留玩家存档。Git分支可通过revert本轮提交回退，禁止为回退强推改写历史。开发目录保持development；main中的basic配置差异应保留。

WorkBuddy从v2.63开发配置接手，先读仓库RELEASE_CHANNELS.md；后续改动先同步到codex/develop，再走main PR并设置basic配置。未向WorkBuddy会话发送消息。GitHub源码推送/构建不代表CrazyGames已审核或发布。

## 代码冻结与浏览器验证补充

- 同步期间另一个任务修改了工作目录中的音效实现；本次GitHub采用已验证的v2.63源码，另存verified/game/index.html。实际工作目录的并行修改完整保留，未反向覆盖；该音效变更不包含在本次提交中。v2.63 Git快照与本次提交主HTML一致，实际工作目录快照可能已被后续任务更新。
- Basic实包英文封面/设置检查：仅语言和音效选项，顶部只有Home；原日志/复位/结束测试/测试倍速/清档入口隐藏。
- 浏览器实际战斗至补给就绪，点击免费领取显示金币到账并立即回到60秒冷却，无广告/模拟弹窗；检查期间控制台无警告/错误。截图见basic-desktop.png、basic-mobile.png。

# v2.40 线上发布接手

2026-09-20，用户已明确要求推送线上。本次没有实际发布：当前 Codex 工具未提供 WorkBuddy 应用发布通道，插件目录查询也没有可接入的 WorkBuddy 插件。

线上核验：`https://neon-hunter.app.workbuddy.host/` 返回 HTTP 200，VERSION 仍为 **v2.39.2**；页面 SHA-256 与修复前基线一致。核验详情见 online-preflight.json。

本地待发布：**v2.40**，主文件与已验收快照散列一致。发布包 neon-hunter-v2.40.zip 仅包含根目录 index.html 和 assets/，共 62 个文件；不含历史版本、日志、测试夹具或工程资料。压缩包完整性及入口内容已检查。

给 WorkBuddy 的可直接使用指令：

> 请将当前 `/Users/zmy/WorkBuddy/CrazyGames/game/` 的 v2.40 更新到现有应用“霓虹猎手”（locator：wbapp_4QnNeb5xhXaduj6Io31tHn），沿用 https://neon-hunter.app.workbuddy.host/，使用 updateExistingApp 重发，不创建新应用或域名。用户已要求推送线上。代码由 Codex 完成并验收，详见 design/qa-v2.40-20260920/修复与验收闭环.md。发布后核验线上 VERSION 为 v2.40，主页面 SHA-256 为 ed25b79144bd34698ef1d43b5e45ea63920c61c1cef4535b84be9783e2aa55d8，并更新发布记录。

该指令只保存在共享文件中，未向 WorkBuddy 会话发送。iPhone Safari 真机验收仍待发布后执行。

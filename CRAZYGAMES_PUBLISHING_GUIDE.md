# CrazyGames 游戏发布与接入规则

核验日期：2026-09-22。整理者：Codex。适用对象：当前《霓虹猎手》HTML5 游戏。

本文区分「平台要求」「官方参考指标」「项目建议」「当前观察」。以所链接的官方页面及提交后台最新提示为准；本次完成规则整理和源码定点检查，没有接入 SDK、修改游戏、提交平台审核或验证线上状态。

实际游戏工程：`/Users/zmy/WorkBuddy/CrazyGames`。本文件位于 `/Users/zmy/Documents/ChatGPT/CrazyGames_1`，作为发布规则与交接资料保存；该目录的旧 Phaser 骨架和搜打撤策划不是当前发布包。

## 2026-09-22 后续实施更新

用户已选择默认英文、保留中文切换。实际工程已升级v2.62：完成372条英文词典、语言设置与动态界面/改装提示覆盖，桌面和手机尺寸检查；本地化14/14、技能38/38、核心75/76（既有C75）。见 [实施报告](/Users/zmy/WorkBuddy/CrazyGames/design/english-v2.62-20260922/修改与验证.md)。下方v2.61源码观察保留为整改前记录；英文任务已实施，但真机/平台验收、SDK locale、模拟广告替代和其余发布准备仍待完成。未提交审核或发布。

## 1. 发布阶段与流程

| 阶段 | 平台要求与行为 | 项目准备重点 |
| --- | --- | --- |
| Basic Launch | 面向有限玩家测试；执行基础 QA；SDK 可选；视频、横幅和内购变现关闭 | 完整可玩的英文版本、稳定加载、无广告也能正常推进 |
| Full Launch | 平台根据表现邀请；完成完整接入和 QA 后面向更广玩家，开放变现分成 | SDK 生命周期、广告、适用的云存档／账号功能、正式平台验收 |

Basic 测试同时满足「上线至少 7 天」和「至少 500 次游玩」后结束；未达到 500 次时最多到第 21 天。达到次数不代表获准 Full Launch；平台还比较时长、留存和进入玩法的转化表现。个别受邀项目可能跳过 Basic，不能默认本项目适用。[官方发布流程](https://docs.crazygames.com/)

建议按以下顺序推进：

1. 在 [Developer Portal](https://developer.crazygames.com/) 完成开发者与 Billing 信息。
2. 从实际游戏工程制作候选发布包，准备名称、英文介绍、操作说明、设备／方向声明及展示素材。
3. 上传并使用平台 Preview／QA 工具检查，再提交 Basic 审核。
4. 观察后台指标、修复影响首局体验的问题；Basic 更新机制按后台与官方指南执行。
5. 收到 Full 邀请后补齐完整接入，再提交 Full QA。
6. 发布后持续维护存档兼容、设备体验和广告异常处理。GitHub 推送属于源码管理，不等于平台发布。

提交需要游戏说明、控制说明及图片／视频等元数据；Basic 与 Full 的功能要求不同，不能将所有可选 SDK 模块都当作基础上架门槛。[要求总览](https://docs.crazygames.com/requirements/intro/)

官方参考指标：平均游玩时长约 10 分钟以上、次日留存约 10%–15%、至少玩 1 分钟的转化率约 80% 以上；优秀案例也强调小于 10 秒的加载体验。这些是改进方向，不是保证过审的固定分数线。[Basic 指标指南](https://docs.crazygames.com/resources/basic-launch-metrics/)

## 2. 技术与玩家体验要求

### 文件和设备

| 项目 | 当前官方要求 |
| --- | --- |
| 全部文件 | 总大小最多 250 MB，文件数最多 1500 |
| 首次下载 | 最多 50 MB；争取移动端首页须不超过 20 MB |
| 下载计量 | 集成 SDK 时从开始加载计至首次真实 `gameplayStart`；无 SDK 时按总文件大小判断 |
| 外部加载资源 | QA 按进入玩法耗时评估，要求不超过 20 秒 |
| 包内引用 | 使用相对路径 |
| 浏览器 | Chrome、Edge 正常运行；Safari 不兼容会限制该浏览器曝光 |
| 低内存设备 | Chromebook 应能在 4 GB RAM 设备顺畅运行 |
| 移动端 | 支持触控、所声明方向、安全区；方向由提交配置控制；移动支持不是所有游戏统一强制 |

iOS 中断音频后，要在有效用户手势中恢复 AudioContext；只监听前后台切换不够。若使用域名锁，需覆盖平台多域名和 App 来源；额外采集个人数据时需提供相应告知。[技术要求](https://docs.crazygames.com/requirements/technical/)

### 内容与操作

平台要求提供英文；多语言按 SDK locale 选择，缺省回退英文。文字在 DPR=1 和 iframe 中应清晰，游戏速度不应随高刷新率变化。Full Launch 的新玩家应直接进入玩法，不能直接进入时最多一次点击。平台提供全屏功能，禁止游戏内自带全屏按钮；内容须原创或有使用权，适合 PEGI 12。外部导流有严格限制，App Store 链接不能放在游戏内，应使用后台字段。[玩法要求](https://docs.crazygames.com/requirements/gameplay/)

项目验收建议：至少检查 821×462、907×510、1280×720、1920×1080，以及实际手机和平台 App。不要把桌面浏览器模拟手机尺寸当成真机验收。优先检查长英文按钮、窄屏技能选择、补给／重塑弹窗和首次进入战斗路径。

## 3. HTML5 SDK 接入边界

当前官方 HTML5 SDK 为 v3，脚本地址 `https://sdk.crazygames.com/crazygames-sdk-v3.js`；使用前需等待 `window.CrazyGames.SDK.init()`。本地 localhost 支持测试行为，最终以平台 Preview 验证为准；非平台域名上的成功运行不能证明正式集成通过。[SDK 初始化与测试](https://docs.crazygames.com/sdk/intro/)

| API／设置 | 含义 | 《霓虹猎手》建议映射，尚未实现 |
| --- | --- | --- |
| `game.gameplayStart()` | 玩家进入或恢复实际游玩 | 首次战斗开始、退出技能／重塑菜单后恢复战斗；避免每帧调用 |
| `game.gameplayStop()` | 进入菜单、暂停或结束一段玩法 | 重塑结算、暂停选择等实际中断点 |
| `game.loadingStart()` / `loadingStop()` | 可选加载事件 | 对应资源加载阶段，不替代玩法事件 |
| `game.settings.muteAudio` | 平台静音，HTML5 Full 接入必需支持 | 与本地静音、广告静音合并处理，平台静音优先 |
| `game.addSettingsChangeListener()` | 接收平台设置变化 | 播放过程中也应用最新静音状态 |

SDK 文档明确：不要仅因窗口失焦或离开游戏区域手动上报 `gameplayStop`，平台会处理该情形。本地仍应执行自身的后台暂停和音频管理，两者职责不同。[Game 模块](https://docs.crazygames.com/sdk/game/)

项目建议：把平台调用放在统一适配层，区分「普通网页／本地测试」「Basic」「Full」。模拟广告必须由明确的开发模式控制；发布构建不能用模拟完成按钮代表真实广告成功。初始化失败要有明确可恢复状态，避免无限加载；保存失败不得通过创建空档覆盖旧进度。

## 4. 广告规则与奖励结算

### 平台要求

- 只允许 CrazyGames SDK 广告；Basic 禁用变现，游戏必须仍可继续，不能留下点击无效的广告入口。
- 插屏用于死亡、关卡结束等自然停顿，不能打断战斗，也不能绑定设置／商店等导航按钮；平台控制频率，最多每 3 分钟一次，不是要求每 3 分钟必播。
- 激励广告由玩家主动选择，清楚标示广告与奖励，提供非广告替代；跳过／普通继续应与观看选项具有相同尺寸、字体、颜色等视觉待遇，不得隐藏或延迟。广告入口不得放在活跃战斗界面，不能连续看多条才给一份奖励。
- 广告拦截器不能阻断基础玩法。横幅限非战斗的有用页面，平均停留至少 5 秒，同屏最多 2 个且不遮挡内容。

来源：[广告要求](https://docs.crazygames.com/requirements/ads/)。现有 60 秒补给冷却属于项目数值，不是官方批准的激励广告频率。

### 回调与程序处理

HTML5 使用 `ad.requestAd("rewarded", callbacks)` 或 `"midgame"`。请求开始就锁定交互并暂停推进；`adStarted` 才开始广告静音。`adFinished` 处理完成，激励奖励只能在成功完成后发放；`adError` 不发奖励并解除广告占用。错误包括 `adsDisabledBasicLaunch`、`unfilled`、`adblock`、`adCooldown`、`other`。[视频广告 API](https://docs.crazygames.com/sdk/video-ads/)

以下为项目实现建议：

1. 每次请求绑定唯一尝试 ID、当前模式及当前结算 ID，防连点、重复回调和旧请求误发奖。
2. 将「奖励资格、奖励变化、该次奖励已消费」设计为可恢复的结算过程；写入失败时保留可重试状态。
3. 广告结束只解除广告自身的暂停／静音原因。若仍在后台、菜单或玩家已静音，不能直接恢复全部战斗和声音。
4. 无广告、取消及 Basic 禁用时回到可操作界面。若 Basic 需要免费补给，须作为明确的游戏规则设计，不得把 `adError` 当成成功回调。
5. 移除当前供测试使用的「模拟完整观看」发奖入口后，再测试真实 SDK 流程。

对当前两个点位的建议：重塑结算处可作为激励广告候选，但普通重塑与广告重塑按钮需同等视觉权重；战斗页补给箱应改为非战斗页面提供广告奖励，或改成不涉及广告的普通补给。自动战斗也属于活跃玩法，不能仅以玩家没有手动攻击为由豁免。具体方案尚未确定，也未改动游戏。

## 5. 云存档与账号

Full Launch 中，有长期进度的游戏须采用一种平台认可的云保存方式：优先 Data Module；也可使用与 CrazyGames 账号关联的自有后端；符合条件时可用 APS。没有用户系统的游戏不必为了接入而额外自建账号。游客必须能玩；自有账号系统接入时不得保留独立外部登录入口。[账号与进度要求](https://docs.crazygames.com/requirements/account-integration/)

Data Module 在 SDK 初始化后使用 `data.getItem()` / `setItem()` 等接口，并需在提交后台开启相应 Progress Save 选项。数据上限 1 MB；同步通常有 1 秒防抖，特殊情况下可到 30 秒。采用它时，CrazyGames 环境的游客和登录玩家都应统一经 Data Module 读写，不能让独立 localStorage 进度同时充当权威存档。登录时已有云档可能替换游客进度，不能无条件覆盖云档。[Data 模块](https://docs.crazygames.com/sdk/data/)

APS 可同步 localStorage／IndexedDB，适合部分已有本地存档项目，但不允许用于有内购的游戏。采用前需在实际平台确认开启方式及同步效果。[APS 说明](https://docs.crazygames.com/other/aps/)

项目建议：无尽／构筑进度、永久解锁和待结算奖励应保持一致性；测试日志另行限量保存，避免挤占云存档空间。应覆盖游客转登录、已有云档、不同设备、切换账号、写失败、旧版本迁移和双模式独立性。本次不决定使用 Data Module 还是 APS，不能把源码中的 localStorage 自动视为已经具备云同步。

## 6. 提交素材与发布包

| 素材 | 官方规格 |
| --- | --- |
| 横版封面 | 1920×1080，16:9 |
| 竖版封面 | 800×1200，2:3 |
| 方形封面 | 800×800，1:1 |
| 预览视频 | 横版 16:9 与竖版 2:3 两种，官方标注 1080p；15–20 秒，最多 20 秒、50 MB |

封面需视觉一致，不加边框、商店标识或「Play Now」等宣传字样，文字仅游戏名；素材须有权使用。视频无声、不人为加速、不放黑屏／黑边／默认鼠标／推广标识，开头用对应静态封面；竖版视频的具体像素以后台校验为准，不套用 9:16。[封面与视频规则](https://docs.crazygames.com/requirements/game-covers/)

项目打包建议：以实际工程的 `game/index.html` 和它需要的运行资源为依据，保持相对路径。排除 `game/versions/`、设计报告、测试截图、源素材候选、Git 文件、依赖缓存和开发记录。输出包应记录版本、文件清单、字节数及校验值；另测进入真实战斗前的网络传输量。不能直接将整个 GitHub 仓库当成游戏上传包，也不能把目录总大小当成已经测得的首次下载量。

平台会托管经 Developer Portal 上传的游戏文件并通过 CDN 分发。[官方托管说明](https://docs.crazygames.com/faq/)

## 7. 收款、内购与账号后台

当前收款文档使用 Tipalti，入口为 Developer Portal → Account → Billing。须完成身份／地址、税务及支付相关字段；可用付款方式依地区而异。最低付款累计额为 €100；合同付款条件为 NET 60，页面说明目前通常争取更早处理，不能把较快到账作为承诺。可以选择暂缓支付完成相关流程。[收款规则](https://docs.crazygames.com/payouts/)

本项目旧记录「Payoneer 已通过」仅是历史用户状态，不能据此认定当前 Billing 已配置完成。本次没有登录支付后台检查。分成比例、地区支付资格、税务与独家条件以账号实际条款为准；不据旧聊天估算固定收益。

内购仅对选定项目开放，需要完整接入并按平台指定的 Xsolla 流程处理，不属于此次广告版本的默认前置任务。[变现与内购范围](https://docs.crazygames.com/requirements/intro/)

## 8. 当前《霓虹猎手》的差距与工作顺序

以下仅基于 2026-09-22 对实际工程的定点读取，源码 `VERSION` 为 v2.61；没有重新做全项目审计或浏览器验收。

| 优先级／阶段 | 当前观察 | 建议下一步 |
| --- | --- | --- |
| P0／Basic | 主页面声明 `zh-CN`，主要操作／技能／弹窗为中文，定点检索未发现完整英文切换 | 准备完整英文界面与说明，复核所有动态文本和窄屏排版 |
| P0／Basic | `requestPrestigeAd()`、补给箱仍采用模拟广告和模拟成功结算 | 制作无模拟广告的 Basic 行为；确定隐藏、替代或免费补给方案，验证不破坏成长循环 |
| P0／发布包 | 当前目录是旧工程，真实游戏在 WorkBuddy 目录，且真实工程含历史快照 | 仅导出当前游戏所需运行文件，测实际首载和总包 |
| P1／Full | 主 HTML 与 JS 检索未见 SDK v3／生命周期调用 | 添加统一适配层，接入初始化、玩法事件和平台静音 |
| P1／Full | 战斗页有可点击的广告补给入口；结算按钮使用不同配色 | 调整广告点位及等权选择，补真实 SDK 异常回归 |
| P1／Full | 进度、模式及日志直接使用 localStorage | 选择 Data Module 或适用的 APS，完成云存档与迁移验证 |
| 待验收 | 已有双模式及大量界面，最新交接仍标记未发布／真机待验 | 复核新用户点击数、iframe 小尺寸、真机、安全区和后台音频恢复 |
| 待准备 | 本次未核验平台封面、预览视频和后台表单 | 按第 6 节制作素材，检查名称、控制说明、Billing |

当前证据入口：

- [实际游戏源码](/Users/zmy/WorkBuddy/CrazyGames/game/index.html)：`VERSION`、`showPrestigeOffer`、`requestPrestigeAd`、`finishPrestigeAd`、补给箱处理及 localStorage 调用。
- [实际项目交接](/Users/zmy/WorkBuddy/CrazyGames/交接文档.md)。
- [最新修改与验证记录](/Users/zmy/WorkBuddy/CrazyGames/design/数值调整记录.md)。历史检查结果不等于 CrazyGames QA 已通过。

建议先做 Basic 候选：英文 → 无模拟广告也完整可玩 → 精简发布包 → 平台 Preview／真机 → Basic 审核。Full 接入可以预先设计；上述修改均为后续工作，本次仅完成规则整理。

## 9. 发布验收记录模板

以下均为待执行项；每项记录版本、环境、实际结果及证据，不能仅勾选完成。

- [ ] 明确本次阶段为 Basic 或 Full，发布包来自正确工程及版本。
- [ ] 英文文本完整，首次进入战斗路径和多尺寸可读性已验收。
- [ ] 发布包的文件数、总大小、实际首载及外部请求符合第 2 节。
- [ ] Basic 无模拟广告和无效广告按钮，普通游戏循环完整。
- [ ] Full SDK 初始化、玩法事件和平台静音行为通过 Preview 检查。
- [ ] 广告成功、无填充、拦截、Basic 禁用、冷却、重复回调均有正确结果。
- [ ] 广告期间暂停推进，后台／菜单／静音并存时恢复正确，奖励不重复。
- [ ] 云存档、游客登录、跨设备、双模式、迁移和存储异常均已验证。
- [ ] Chrome／Edge、目标手机、iframe／全屏和 App 安全区已覆盖。
- [ ] 图片、视频、英文介绍、控制说明、素材使用权及 Billing 已核对。
- [ ] 平台审核与发布结果单独记录，保留可回退的候选包及存档兼容说明。

更新规则：正式提交前重新核验本文链接；将变化记录为新日期。平台明确要求优先于本文摘录，项目建议须与已确认玩法分开管理。
